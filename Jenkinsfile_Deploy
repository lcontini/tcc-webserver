node('master') {

stage 'Checkout'
 checkout scm

stage 'Push Versao'
 sh 'ssh -tty tcc-webserver1 sudo rm -rf /var/www'
 sh 'ssh -tty tcc-webserver1 sudo git clone -b dev https://github.com/lcontini/tcc-webserver.git /var/www'
}
