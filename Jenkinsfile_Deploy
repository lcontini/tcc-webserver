node('master') {

stage 'Checkout'
 checkout scm

stage 'Push Versao'
 sh """
   ssh -tty root@tcc-webserver1 '
	source ~/.bash_profile;
	rm -rf /var/www; 
	git clone -b dev git@github.com:lcontini/tcc-webserver.git /var/www'
 """
}
